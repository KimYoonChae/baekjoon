#include <stdio.h>
int fib(int n) {

    if (n ==1 || n == 2) return 1;  
    else return (fib(n - 1) + fib(n - 2));
}
fibonacci(int n) {
    f[1] <- f[2] <- 1;
    for i <- 3 to n
        f[i] <- f[i - 1] + f[i - 2];  # 코드2
    return f[n];
}
int main(){
    int a=0,b=0;
    int num;
    scanf("%d",&num);

    







    

}